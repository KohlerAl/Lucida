1. Get the Angle and the distance between Start and End Point
->  var x = x2 - x1
    var y = y2 - y1

    distance: Math.sqrt(x * x + y * y); 
    angle: Math.atan2(x, y) * 180 / Math.PI 
 
2. Set Velocity: 2 (could also be depending on distance)

3. Create Vector with Velocity and angle (between start and end)
->  var angleRadians: (angle * Math.PI) / 180
    speedX : velocity * Math.cos(andleRadians); 
    speedY : velocity * Math.sin(andleRadians); 